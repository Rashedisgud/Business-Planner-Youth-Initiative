# Copy this file to .env and adjust if your backend runs somewhere else.
VITE_API_URL=http://localhost:4000

# Supabase (Project Settings -> API). The anon/publishable key is safe to
# expose in frontend code - it is NOT the service role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
